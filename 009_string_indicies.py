my_name = "Timothy Unkert!"

# print the first character of the string to the console
print(my_name[0])

# print the last character of the string to the console
print(my_name[-1])

# with indices of a string you can't change them, they are immutable
# my_name[0] = "J"  # this will throw an error
